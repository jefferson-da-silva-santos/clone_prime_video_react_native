import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  groupButtons: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    gap: 16,
    justifyContent: 'space-evenly'
  },
  button: {
    // ainda não tem nada, mas pode ser usado para colocar um background ou algo do tipo
  },
  textButton: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '500'
  }
});